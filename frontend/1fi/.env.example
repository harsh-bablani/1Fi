# Copy this file to .env and update values before running locally or deploying.
# When deployed to Vercel, set `VITE_API_BASE_URL` in the Project Settings > Environment Variables.

# Backend API base URL (must include /api)
VITE_API_BASE_URL=https://onefi-pszm.onrender.com/api
